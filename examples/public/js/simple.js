// Simple example without context data
const root = document.getElementById("root");

root.innerHTML = `
    <div class="simple">
        <h1>Simple Example</h1>
        <p>This page doesn't use any context data.</p>
        <p>It demonstrates basic rendering without server-side data.</p>
    </div>
`;
